# M6.5：Primary Reduction Selection

**Primary Candidate: C1 — k* context occurrence reduction。**

**Secondary Candidate: C2 — pk_e context reduction，作为 future work。**

状态：主研究对象已选择，等待 M6.5 审查。选择研究对象不等于字段可删、安全准入或获准开始 theorem proof/Tamarin。

## 1. 选择对象的精确范围

C1仅研究原 c_KEM[8] 的 k* 出现位置；原 σ_KEM=k*、其他context位置、QKD输入及接口、KEM/MAC算法与调用、报文、label、source顺序、输出拆分和失败规则不变。原与候选公式见 [比较分析](reduction-target-selection.md) §1。本轮没有修改公式的实现，也不选择C1+C2组合。

“保留MAC计算”指规则、消息表达式和key来源规则保持，不预设F输出、确认tag或双方接受结果逐字一致。候选的角色本地值不合并为先验相等。

## 2. Selection reason

| 决策维度 | 选择C1的依据 | 必须接受的限制 |
|---|---|---|
| 研究问题清晰度 | 单个同侧重复位置与σ保留给出明确的问题：已有secret输入及协议记录是否足以承接该出现位置的候选职责？ | 元组层可恢复观察不是安全保持结论，不覆盖任意RO查询或全部会话关系 |
| 证明复杂度 | 相比C2，首轮不必以额外跨pk_e绑定假设作为候选动机，可先解决共同source/query/确认接口 | 不保证无需KEM相关论证；B1/B4及模型语义仍可能阻断全部路线，没有工时/成功率估计 |
| 安全性质覆盖 | 可将原Key-IND、matching正确性、PFS及混合失败范围作为同一个HAKE位置级问题审查 | 全部分支仍未获证明；不能以BG或QS成功代替原合同，更不能默认QT成立 |
| 创新性 | 潜在贡献是HAKE-specific的secret/context接口到认证会话/组合语义的证据链，而非字符串层面的精简 | 原语重复本身可能过于简单；必须证明现有工作不能直接实例化，或诚实地将成果定位为实例化/接口边界研究 |

综合判断：C1是**较集中的首个研究对象**，不是“最安全的候选”或“必然可发表的优化”。C2的跨层绑定主题更直接，但同时增加K-PK适用域、临时凭据认证和失效分支可用性问题，暂不并入主线。

## 3. 主路线保留的义务与停止条件

- SP-01 / OI-01–OI-06：所需事件、freshness、历史Test和擦除规则仍需原文依据或明确人工裁决，不导入CK01/Boyd默认规则。
- SP-02 / B1：原与候选均要有合法source/真实α解释。C1没有解决k1/k2秘密context，不能公开这些值或改σ来补洞。
- SP-03 / B3/B4：合法记录、匹配双方、重复查询、QKD ID/source索引和允许泄漏必须一致。
- SP-04 / B7：确认半段和最终半段的联合视图需独立处理，不循环引用τ3。
- SP-05 / B2/B6：逐分支核对存活保证、预测/碰撞余项及oracle能力；损失仍unknown。

若这些共同接口不能闭合，应暂停正面保持主张，记录基线适用边界，而不是为了继续优化而修改HAKE或增强假设。若仅较窄分支可支持，也只能报告较窄条件结果，需人工确认其是否仍符合研究目标。

## 4. C2的后续定位

C2保留为独立的future work：在共同source和模型接口清楚后，调查具体K-PK到ephemeral session binding的非循环桥及其失败范围。C1将来即便成功，也不自动提供C2的凭据桥；分别成功也不推出联合保持。

重新选择主路线的依据应是新证据，例如C1仅为已知结果的直接实例化且不足以形成预期贡献，或出现完全适配HAKE模型/分支的C2证明接口。不是因希望获得更强结论而跳过当前阻塞项。本轮不启动这些后续工作。

## 5. 审查请求和阶段出口

本次请求审查的是：是否接受C1为首个HAKE-specific研究对象，C2后置；是否认可候选比较及贡献边界。**不请求把开放模型项默认为已解决。**

当前M7前提仍未满足。本轮只完成research target selection；不写定理、不构造证明、不运行Tamarin、不删字段。完成三份M6.5文档后停止。

文档检查：三份新增文件的本地链接和代码块围栏通过检查，C1/C2主次选择一致；既有M2–M4校验及10份来源哈希检查通过。工作区仅新增指定三份文件，没有commit/push。检查结果不构成安全验证。
