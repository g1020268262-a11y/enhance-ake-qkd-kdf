# 基于安全性质的 KDF context 删减：论文执行大纲

版本：2026-09-18。用途：按任务推进、记录证据、判断何时能够开始形成论文主结果。

## 1. 论文究竟要完成什么

第一篇的唯一协议对象是 Clermont–Henrich 的 `Π_HAKE`。研究目标是忠实重建其 KDF context，逐字段分析显式 binding 的安全作用，检查 KEM binding、KEM-AKE authentication、authenticated setup 等原有保证是否在**该 HAKE 协议的实际模型与故障分支**中足以替代某个字段出现位置，构造具体 reduced HAKE 变体，并重新证明该变体在明确假设下保持哪些原目标性质。不得预设任何字段可删。

第一篇的研究闭环是“HAKE baseline → 字段职责与替代依据 → 具体删减变体 → 安全保持证明 → 不可删/条件受限的边界”。字段对照表和性能收益都不能单独代替 HAKE 变体的安全结果。若分析自然形成可复用充分条件，可在讨论中记录；“已有 primitive/protocol guarantee 何时一般地替代显式 KDF context binding”的跨协议定理属于后续第二篇，不是第一篇的前置任务或最低成果。

暂定题目：**HAKE 协议的 KDF 上下文安全保持型删减分析**。英文工作标题：**Security-Preserving KDF Context Reduction in a KEM–QKD Hybrid AKE**。在证明成立前，标题不使用“最小”“最优”“无损”等已经包含结论的词；若没有正面删减结果，须据实际边界结果调整题目。

最低研究闭环应包含：

1. 准确的 HAKE baseline、字段安全职责、原模型与故障分支合同。
2. 一个明确的 HAKE-specific context 投影及对应变体；先分析单字段，联合删减时重新检查所有依赖与安全分支。
3. 对该变体给出非循环的正确性和目标安全保持证明，逐项列明攻击者、freshness、KDF/source 条件、新增假设及损失界；**不要求先证明一般协议类定理**。
4. 至少一个 HAKE 内的不可删、条件不足或安全范围收缩的边界结果，并准确区分反例、证明条件不满足与真正的不可能性。若无字段被证明可删，转为边界研究，不宣称优化成功。
5. 原版与变体的可追溯规范、证明记录及 security ledger；精确输入成本可记录，形式化工具与 benchmark 只是支持性证据。

第二个协议实例和跨协议 context-elision 定理属于后续第二篇路线，不是第一篇投稿门槛。上述闭环也不等于保证录用，创新性还需要最接近文献的比较。

资源优先级固定为：**HAKE 单字段安全论证 > HAKE 故障分支保持证明 > HAKE 边界/反例 > protocol-level formal validation > benchmark**。只有前一层存在明确结果或具体阻塞点，才投入后一层。

## 2. 本次依据及当前完成度

当前仓库中的固定证据是 `2026-1231.pdf`；M0 的范围与主张登记见 `research-scope.md`、`claims-register.csv`。路线图保留 Hybrid 分支早期门槛、单字段优先、边界分析及验证/实验的支持性定位；2026-09-18 将第一篇从“一般定理 + HAKE corollary”调整为 HAKE-specific 安全保持分析。任何示例定理、示例删减式和“最小 context”仍只是待证设想。

| 项目 | 当前可以确认的状态 |
|---|---|
| 研究方向 | 已明确：`Π_HAKE` 的 KDF context 安全保持型删减分析；一般化留待后续第二篇 |
| 原始对象 | Clermont–Henrich 的 HAKE；来源见文末 |
| 待考察字段 | 已有候选，并未证明可删 |
| HAKE 变体及安全保持条件的精确定义 | M0 只定边界；待 M1/M3/M5 完成 |
| HAKE-specific 安全证明与归约 | 需要完成；不预置一般协议类定理 |
| 形式化模型和结果 | 本次未核验到可作为成果的代码、日志或证明 |
| 创新性 | 存在必须比较的近邻工作，尚不能宣称“首次” |
| 性能收益 | 未测量 |

特别注意五个修正：

- 不能从“某绑定关系在原协议接受时成立”直接推出“删除该字段后安全”；原绑定关系可能正是依赖待删字段才成立。
- 不能把 C2PRI、K–PK binding、K–CT binding 当作同一性质；要保留每个游戏的诚实生成、固定输入、恶意选择和泄漏条件。
- 不能把“逐个删除均安全”推成“全部同时删除也安全”；删减可能存在相互依赖。
- 不能用计算性绑定证明直接继承面向无界攻击者的保证；必须分别检查原论文的条件信息论声明及其理想化模型。
- 任何候选进入 HAKE 安全保持命题前，必须先通过 Hybrid failure-branch compatibility gate：删除依据在 KEM 保密性失效、KEM binding 也失效以及 QKD-only 分支中是否仍可合法使用。若只能继续假设 KEM binding，就必须把结果写成更强假设下的条件保持，不能称为完整继承原 Hybrid 保证。

## 3. 推进规则

每个阶段建立一张任务卡，填写：输入材料、执行步骤、产物、验收标准、未解决问题。状态只用：未开始、进行中、已验证、失败、范围外。没有证据不得标为已验证。

任何字段状态只用：已证明可删、在附加条件下可删、已有反例、证据不足。证明失败不等于字段必要。

每条安全结论至少记录：协议版本、字段集合、攻击模型、freshness、假设、目标性质、证据位置。修改任一项后重新检查受影响结论。

M0/M1 产物已创建；M1 未完全冻结。M2 原始文献核查、矩阵与差异审查已交付，M3 安全模型、分支合同及四位置准入表已交付；M3.2 的原模型精确冻结仍有来源冲突。详见 `m2-m3-status.md`。勾选表示相应核查/登记任务已执行，不表示原安全声明或字段删减已被独立证明；M4 及以后未启动。

## 4. M0：锁定研究问题和范围

**回答的问题：你准备证明的究竟是哪一种“不降低安全”？**

- [ ] M0.1 写一页 `research-scope.md`：研究对象、原模型、允许变更、目标性质、非目标。**内容已具备，但当前文件明显超过一页；按原篇幅要求记为部分完成，不降低验收标准。**
- [x] M0.2 定义第一轮允许的变更：只删指定 KDF context 的字段出现位置；保留消息、MAC 输入、QKD 接口、secret 输入和输出分割。若后来需要改变这些，另立变体并单独核算假设和成本。
- [x] M0.3 建立“原论文已形式证明 / 文字讨论 / 本研究新增目标”三列性质表。不得把期望性质写成原论文已证明性质。
- [x] M0.4 将主目标定为同一模型中的 SK security、该模型覆盖的 PFS、原有混合安全分支，以及与实际角色和完成时点一致的认证/确认保证。
- [x] M0.5 对 identity/role agreement、贡献一致性、会话分离、key independence、transcript integrity 作**研究定位**：区分原 HAKE 主目标、删减证明的辅助不变量/Bad event 与扩展性质。现有事件文字只是草案；严格事件和游戏、接受时点及多 Test freshness 留待 M1/M3/M5 固定。
- [x] M0.6 PCS 暂不纳入主线；不无依据地加入协商降级、QKD key 重复输出等与原模型不符的能力。
- [x] M0.7 区分两种主张：同一目标性质在新增假设下成立；原有故障容忍和攻击者范围都保持。前者不能替代后者。

产物：`research-scope.md`、`claims-register.csv`。M0 范围内容与研究定位已交付；M0.1 的一页篇幅要求仍未满足。严格形式定义、M1/M3 细化和任何删减证明尚未开始。M0.5 的勾选只代表**定位完成**，不代表事件/游戏已形式化。

验收：能够完整回答“谁攻击什么版本、能做什么、哪些会话可挑战、最终赢什么游戏”。“协议安全不变”不是合格答案。

## 5. M1：忠实重建原协议与 KDF 数据流

**回答的问题：每个字段在哪里产生、认证、使用，它为什么进入 KDF？**

原文给出的关键结构为：

\[
\sigma_{\mathrm{KEM}}=k^*,\qquad
c_{\mathrm{KEM}}=A\|pk_a\|B\|pk_b\|pk_e\|k_1\|k_2\|k^*.
\]

\[
\sigma_{\mathrm{QKD}}=k_{\mathrm{qkd}},\qquad
c_{\mathrm{QKD}}=A\|B\|qkdKeyId.
\]

这里的 context 含秘密值，不能笼统当作公开 transcript。删掉一个秘密在 context 中的出现位置，也不等于可以从协议或 secret 输入中删除它。

- [x] M1.1 固定原 PDF 版本、引用页码、公式和图号；记录正文、图和实现若有不一致，暂不擅自选择有利解释。见 `baseline-spec.md` §1、§9。
- [ ] M1.2 按原文重画消息流程；标出每一侧的发送、检查、派生、接受、失败和擦除时刻。**部分完成**：Figure 3 流程和显式失败已重建；正式 Accept 与中间状态擦除未给出，见 `baseline-spec.md` O3–O5。
- [x] M1.3 为 A、B、静态公钥、临时公钥、各密文、k1、k2、k*、QKD key 和 ID 建立符号表。见 `baseline-spec.md` §3 与 `field-ledger.csv`。
- [x] M1.4 建立 MAC 覆盖表：每个 tag 的密钥、完整编码输入、生成者、验证者，以及保护在什么时候生效。见 `baseline-spec.md` §5；实际字节编码的不明确处另记 O9。
- [ ] M1.5 分离 KDF 的 secret、component context、全局 label、输出长度和派生用途；逐层记录实际编码与调用次数。**部分完成**：图示顶层调用已记录；精确序列化、内部调用次数和 SHA-3 实例存在原文缺口，见 `kdf-dataflow.md`。
- [x] M1.6 标明哪些是线上传输字节，哪些只是本地 KDF 输入，哪些在其他层已被散列或认证。见 `kdf-dataflow.md` 与 `field-ledger.csv`；没有将 MAC-key 依赖写作直接消息覆盖。
- [ ] M1.7 核对 authenticated setup：谁保证 identity–public-key 关系，是否要求唯一归属、是否允许共用公钥。**部分完成**：论文给出可信绑定与多种分发方式，未明确单射/共用公钥语义；见 `baseline-spec.md` §2。
- [ ] M1.8 核对 QKD 一次输出、失败返回、端点绑定和可信认证通道的原始假设。**部分完成**：Definition 7 与黑盒范围已记录，内部 endpoint–ID 绑定机制未明示；见 `baseline-spec.md` §7、O8。
- [ ] M1.9 核对原文单向 key confirmation 的实际方向及两侧接受时点；不能自行升级为双向确认，也不能把产生 tag 视为对方已验证。**部分完成**：方向与派生/验证顺序已确认，正式 Accept 时点未定义；见 `baseline-spec.md` §6、O3。
- [x] M1.10 给出一条诚实执行 trace，并逐步说明双方为何得到相同最终 key。见 `baseline-spec.md` §8；这不是安全证明。

产物：`baseline-spec.md`（内含 Figure 3 消息图）、`field-ledger.csv`、`kdf-dataflow.md`。上述部分完成项依赖原文未明示信息或所引原文，不能仅凭 M1 推断补齐。

验收：所有字段和接受判断均有原文依据；没有把项目源的示例删减式作为原协议。

## 6. M2：查新并收缩到真正未解决的问题

**回答的问题：与已有 primitive/combiner 输入省略结果相比，HAKE 具体变体的协议级安全证明新增什么？**

本次已核对 CFRG 的 `draft-irtf-cfrg-hybrid-kems-11`：其中已有省略 PQ KEM 公钥/密文的构造及相应底层绑定讨论。因此“利用 binding 少放几个字段”不能独自作为创新性陈述。该文档是工作草案，引用时固定版本，不称作已发布 RFC。

- [x] M2.1 读 HAKE 和它实际使用的 Boyd 等 KEM-AKE 原始证明，找到认证与 partnering 的准确结论。
- [x] M2.2 读 Backendal 等的 multi-input KDF 框架，尤其 source/context 接口、相关输入、安全游戏、适用构造和归约条件。
- [x] M2.3 读 KEM binding 原始定义：至少覆盖实际候选字段所需的 HON/MAL/LEAK 情形。
- [x] M2.4 比较 X-Wing/Starfighters 及 hybrid-KEM 草案中的输入省略结果，辨别其目标是 IND-CCA、binding 还是协议认证。
- [x] M2.5 查看与 KEM-AKE 身份、贡献绑定有关的形式化工作，仅在它影响 HAKE 变体的证明义务时扩展阅读。
- [x] M2.6 每篇文献填矩阵：研究层次、攻击者、公钥来源、泄漏、删除字段、保留性质、证明方法、与本研究重叠点。
- [x] M2.7 写一段可被证伪的 HAKE-specific 差异陈述，例如：“研究现有 primitive/combiner binding 能否支持 `Π_HAKE` 在身份、角色、会话、QKD 故障分支及腐化时序下的具体 context 删减”。这只是待文献审查支持的候选差异，不称为一般框架。
- [x] M2.8 检查已有定理是否直接覆盖 HAKE；若能直接套用且无新增困难，降低创新预期或调整问题。

产物：`related-work-matrix.csv`、`novelty-gap.md`、准确 BibTeX。

2026-09-18 交付：上述文件及 `references.bib`、`sources/README.md` 已创建；核查 8 项工作（KDF 另保留两版）。G1 结论：指定文献未直接覆盖 HAKE 的投影，但已有 combiner 省略结果排除了泛泛创新口径；B1–B7 给出可核验的 HAKE 证明衔接义务，不宣称首次或漏洞。

验收：能指出最接近工作的具体定理、它的限制和 HAKE 变体新增的证明义务；不能只写“尚无人研究 HAKE”，也不以一般化定理作为第一篇创新性前提。

## 7. M3：固定威胁模型和安全合同

2026-09-19：M2 已经用户审查通过 G1，保持 HAKE 特定衔接目标；M3.2 closure 审查已交付，但存在 OPEN ISSUE，不能自行选择参考语义完成冻结。

**回答的问题：删减在什么攻击者能力下仍有效，替代保证是否兼容原 Hybrid 故障分支？**

- [x] M3.1 逐项抄明原模型 oracle 及返回值：消息控制、会话启动、长期腐化、会话状态泄漏、会话 key reveal、Test。
- [ ] M3.2 定义 session identifier、partner relation、freshness、完成与过期，以及腐化/擦除的时间条件。**OPEN ISSUE（2026-09-19）**：H、R-CK、R-Boyd 分开映射；来源与明示规则已锁定，OI-01–OI-06 等待人工审查，完整游戏未冻结。见 `security-model.md`、`model-assumption-table.csv`、`m3-conclusion.md`；本轮不进入 M4。
- [x] M3.3 主模型保持原协议的 authenticated setup 和 QKD 接口；恶意注册、身份共用凭据、状态回滚若原文不支持，只放在明确标记的扩展或抽象反例中。
- [x] M3.4 区分静态密钥泄漏、临时密钥泄漏、KEM 保密性失效、KEM binding 失效；它们不是同一攻击。
- [x] M3.5 为每个分量建立故障表，分别考察双方安全、只有 KEM 路径可用、只有 QKD 路径可用、全部失效。
- [x] M3.6 对“QKD 路径可用”继续区分 QPT 与原论文条件信息论声明下的攻击者；保留其随机预言机等模型限制。
- [x] M3.7 逐项说明目标安全性质在各分支是否原本承诺成立。没有原保证的格子不要擅自补上。
- [x] M3.8 如新增 binding 假设，写清它限制的是原语实例、攻击者、故障种类还是全部三者。
- [x] M3.9 对每个拟删字段执行 **Field Elision Compatibility with Hybrid Failure Branches** 早期门槛：先填完下表，再允许该候选进入 M4 的证明路线。
- [x] M3.10 明确定义“broken KEM”：只允许 IND-CCA / secrecy 失效，还是连 C2PRI、K–PK、K–CT 等 binding 也可失效。不得在 QKD-only 证明中把已允许失效的 binding 偷渡回前提。
- [x] M3.11 若删减只能在“secrecy 可失效但指定 binding 仍保持”下成立，立即建立独立、较窄的安全合同并修改贡献口径；不得等到 M7 才披露保证变化。

建议的安全合同表：

| 分支 | 存活的安全来源 | 额外绑定从哪里来 | 目标性质 | 删除后需交付的证据 |
|---|---|---|---|---|
| 两分量均安全 | KEM-AKE、QKD | 分别核查 | 原模型目标 | 基本保持证明 |
| QKD 失效 | KEM-AKE | 不可借用理想 QKD 结论 | 原先承诺的计算安全 | KEM 分支证明 |
| KEM 相关保证失效 | QKD | 不可暗中重新假设 KEM binding | 原先承诺的混合安全 | QKD 分支证明 |
| 原文条件 ITS 分支 | 原文 QKD 与理想化模型 | 必须在该攻击者类下有效 | 原文准确声明 | 单独的模型与界审查 |

候选字段的早期兼容性表必须逐字段填写，不能用协议级总评代替：

| 拟删字段/位置 | 删除依据 | KEM 正常 | KEM secrecy 失效但 binding 保持 | KEM binding 也失效 | QKD-only 分支是否仍成立 | 初步判定 |
|---|---|---|---|---|---|---|
| context 中重复的 k* | 待确定的 source/context 条件 | 待核验 | 待核验 | 待核验 | 待核验 | 未准入 |
| pk_e | C2PRI 或实际需要的 K–PK/K–CT 性质 | 待核验 | 待核验 | 待核验 | 待核验 | 未准入 |
| pk_a、pk_b | K–PK binding + authenticated credential/agreement | 待核验 | 待核验 | 待核验 | 待核验 | 未准入 |

上表保留为初始模板，**实际核查结果以 `hybrid-branch-compatibility.csv` 的 E01–E04 为准**：四个位置均为“证据不足、未准入”；静态 pk 已分别审查。C2PRI 不能充当 pk_e 的 K–PK 证明。`security-contract.csv` 已区分 BG/KO/QS/QB/QT/FF；`C-BIND` 作为待证较窄合同登记，尚未启用。首轮保留 k1、k2、A、B、qkdKeyId，未将其列为本轮拟删位置。

判定只允许写：全部原分支兼容 / 仅附加 binding 条件下兼容 / 不兼容 / 证据不足。只有第一类可进入“完整保持原保证”的主张；第二类必须改成较窄安全合同，后两类不得作为正面删减结果。

产物：`security-model.md`、`security-contract.csv`、`hybrid-branch-compatibility.csv`。

2026-09-18 判定：分析材料已交付；G2 未通过，不启动 M4 正面删减。M3.2 与 baseline/KDF 接口义务仍开放。Thm.1–2 的原文声明保留，但 KDF 公开 context、预测余项、RO 能力及事件映射未独立闭合，不能以“原文已有定理”跳过这些问题。

验收：每个拟删字段的证明都能定位到安全合同中的格子，并已明确替代假设在 KEM binding 失效时是否仍可用；如果只覆盖部分格子，就只能声称部分保持。

## 8. M4：建立字段—性质—证明依赖图，选择首个候选

**回答的问题：一个字段的所有作用是否都有独立、足够强的替代保证？**

- [ ] M4.1 每个字段分别列出 identity、credential、role、contribution、session、domain、key confirmation、source separation 的潜在作用；通过原证明确认，不能凭名字判定。
- [ ] M4.2 对每种作用查找现有 primitive 或 protocol 保证，并记录该保证的精确攻击游戏。
- [ ] M4.3 检查保证生效时点：派生之前、发出确认之前、验证确认之后，还是只有应用消息之后。
- [ ] M4.4 画证明依赖图，发现“要删除 c，却用包含 c 的最终确认来证明可以删除 c”之类循环。
- [ ] M4.5 标明所有依赖字段：删除 pk_a 的论证是否还需要 A、k2；后续若删除它们，前一证明是否失效。
- [ ] M4.6 选择一个低复杂度单字段变体做第一轮研究。优先审查 context 中重复的 k*，再审查 pk_e；这只是工作排序，不预判可删。
- [ ] M4.7 首轮保留 identity、QKD ID、全局 label 和协议状态规则，以缩小证明范围；不是宣称它们永远不能删除。
- [ ] M4.8 只从 M3 早期兼容性门槛已通过、或已明确接受“较窄附加假设”口径的字段中选首个候选；门槛结论必须写入 `candidate-01.md` 首页。

| 候选 | 必须回答的问题 | 主要障碍 | 当前状态 |
|---|---|---|---|
| context 中重复的 k* | 已作为 secret 输入，另一处出现是否承担 source/context 游戏要求？ | 多输入 KDF 的位置和相关输入语义 | 待证明 |
| pk_e | k* 的绑定在实际封装/解封装、腐化和故障分支中够强吗？ | 恶意 key、时间条件、混合失效 | 待证明 |
| pk_a、pk_b | KEM key 与公钥以及公钥与身份的链条是否完整？ | credential 归属、角色、多会话 | 待证明 |
| k1、k2 | 它们是否还承担认证秘密、source 区分或确认功能？ | 秘密相关性、认证依赖、循环 | 待证明 |
| A、B | 哪一层真正保证逻辑身份一致？ | KEM binding 不直接表达身份语义 | 待证明；首轮保留 |
| qkdKeyId | 与端点、一次性 key 和会话状态的联系由哪里保证？ | 破坏已有 component/session matching | 待证明；首轮保留 |

产物：`binding-matrix.csv`、依赖图、`candidate-01.md`。

验收：能说明“删哪一个出现位置、靠什么保证、在哪个分支成立、替代保证失效时怎样处理、可能怎样失败”。

## 9. M5：定义 HAKE 删减变体与可证明命题

**回答的问题：对原 HAKE 的哪个 context 出现位置做了什么投影，何种条件足以证明该具体变体安全？**

记原协议为 `Π_HAKE`，待删的具体 context 出现位置集合为 D，变体为 `Π_HAKE−D`。性质集合 S、攻击模型 M 与假设 H 都必须固定为该 HAKE 分析的可核验对象；不能说一个字段“绝对冗余”，也不在本篇定义任意协议类别的通用 context-elision 框架。

- [ ] M5.1 形式定义删减转换：在哪一层、哪些调用、哪些角色、哪些位置应用投影；保证编码唯一解析和标签不混用。
- [ ] M5.2 定义安全保持：对于指定 S 和 M，存在有效归约及明确损失界。新旧 key 的比特值通常不同，不要求逐次相等，也不要求任意外部环境下完全等价。
- [ ] M5.3 定义候选的 implicit context binding。必须跨执行或跨会话表达需要区分的语义，而不只是同一方局部“知道一个字段”。
- [ ] M5.4 对保留派生输入相同而被删语义不同的情况定义 Bad 事件；明确它涉及已派生、接受、还是可被 reveal/query 的未完成会话。
- [ ] M5.5 如使用“保留输入决定被删字段”，说明是数学唯一性、可计算恢复还是对有效攻击者的不可碰撞；三者不可互换。
- [ ] M5.6 给出实际 KDF/source 游戏需要的附加条件。一般的绑定关系不足以证明任意 F 在删输入后仍安全。
- [ ] M5.7 将前提绑定到 Figure 3 的 HAKE 流程、原文 authenticated setup、KEM-AKE/QKD source 和实际 multi-input combiner；不为本篇额外建立一般协议类。
- [ ] M5.8 先提出一个 HAKE 单字段变体的安全保持命题，并给出该变体满足前提的证据及不满足条件时的 HAKE 内边界；不要求抽象协议正例。
- [ ] M5.9 如研究联合删减，每次投影后重新验证剩余前提、依赖字段和安全分支，并直接证明联合变体或给出适用于这些 HAKE 变体的迭代论证；不从各字段分别可删直接推出联合可删。
- [ ] M5.10 区分“充分条件不满足”和“删除不安全”；前者只能标为证据不足，除非有攻击或不可能性证明。

可用作启发而非结论的关系

\[
\Pr[\neg R_i(\sigma,c_i)\land Accept]\leq\operatorname{negl}(\lambda)
\]

只能当作待加强的思路。它本身没有约束 KDF 转换、跨会话别名、未完成会话查询和循环认证，也没有证明覆盖混合失效分支。

产物：HAKE 变体定义 `definitions.md`、`hake-single-field-proof-draft.md`；确有联合删减主张时再增加对应的联合变体证明草稿。

验收：定义能判定一个实际 HAKE 单字段变体及其边界；前提没有把“删减后目标安全”直接当作前提。

## 10. M6：先构造攻击假设，再检查候选删减

**回答的问题：攻击者如何利用被删除的区分信息，让某方接受错误语义或破坏 key 安全？**

- [ ] M6.1 每次只建立一个清晰变体：原版、删重复 k*、删 pk_e、其他单字段；避免混杂多个改动。
- [ ] M6.2 先用诚实 trace 排除正确性错误。
- [ ] M6.3 检查并发会话拼接、身份/凭据混淆、角色反射、贡献替换、允许的腐化前后行为、未完成会话与 reveal 交互。
- [ ] M6.4 在安全合同每个适用分支中重复逻辑分析；关键问题是“提供替代绑定的分量失效时还剩什么”。
- [ ] M6.5 如构造“两个身份共用同一 key”的分离例，先核对该设置是否被 HAKE 模型允许。范围外反例只能作为后续一般化研究的边界，不能宣称攻破原 HAKE 或证明本篇变体不安全。
- [ ] M6.6 要区分实际会话攻击与离线构造相同 KEM key 的异常元组；后者必须接入接受事件才能成为协议攻击。
- [ ] M6.7 每条反例回到原始消息、MAC、QKD 接口逐步核验，排除漏建认证、漏建一次性状态导致的伪攻击。
- [ ] M6.8 将负结果设为独立研究目标，而非证明失败的附注：先在 HAKE 的允许能力下检验候选 primitive binding（例如 C2PRI）是否不足以支撑 identity、role 或 session context 删减；若反例必须扩展模型，清楚标为抽象边界。
- [ ] M6.9 分离结果必须准确写成“某组原语性质不足以推出某类 context 可删”；除非覆盖所有替代条件，不得升级成“该字段在任何设计中都不可删”。

统一攻击卡模板：

| 项目 | 必须填写 |
|---|---|
| 攻击假设 | 删除哪个字段可能允许什么混淆 |
| 所需能力 | 精确 oracle、恶意输入、注册或腐化条件 |
| 违反性质 | 一个明确游戏或事件断言 |
| 具体 trace | 初始状态、每条消息与替换、双方认知、接受点、坏事件 |
| 形式验证目标 | 被否定的 lemma 或可执行攻击 trace |
| 模型适用性 | 原模型 / 明确扩展 / 仅抽象分离例 |
| 结论状态 | 候选 / 排除 / 已复现 / 仍需计算可实现性 |

产物：`attack-hypotheses.md`、`separation-results.md`、最小反例记录。

验收：攻击必须以合法能力抵达违反性质的接受或挑战状态；“我能找到碰撞”不算完整 trace。

## 11. M7：完成具体 HAKE 变体的安全保持证明

**回答的问题：每一个安全损失都能归结为什么原语事件或协议事件？**

- [ ] M7.1 从单字段删减后的 `Π_HAKE−D` 实际协议游戏出发，写全 HAKE-specific 安全保持命题的前提、量词、攻击者类和查询界；第一轮固定 \(D=\{x\}\)。
- [ ] M7.2 列出 Bad 事件：语义碰撞、凭据/角色混淆、非唯一匹配、认证伪造、相关 source 异常、状态规则失败。只保留实际相关者。
- [ ] M7.3 将 Bad 事件分别归约到准确的 KEM binding 游戏、认证保证、状态条件或 KDF 性质，记录多用户/多会话损失。
- [ ] M7.4 在排除 Bad 后建立 session 与 source/context 的对应；覆盖未接受但可影响后续执行的会话。
- [ ] M7.5 重证 KDF 输入修改的合法性：能否直接调用原框架，还是必须新增 source admissibility/combiner 引理。
- [ ] M7.6 证明 SK 不可区分性，并独立说明双方正确性；不能用“公钥仍绑定”替代二者。
- [ ] M7.7 核对原证明中的认证、key confirmation 和 partnering 使用了哪些 KDF 值，重新处理受影响部分。
- [ ] M7.8 按原 freshness 和擦除模型证明 PFS，不临时排除原来允许的腐化查询。
- [ ] M7.9 分别证明 KEM 存活和 QKD 存活分支。若一个分支必须依赖已允许失效的 KEM binding，应承认不能继承相应鲁棒性并重新设计删减集合。
- [ ] M7.10 单独检查条件 ITS 声明：计算性 negl 项对无界攻击者不够；核对 ROM/QROM、查询界及具体 SHA-3 实例与理想模型的区别。
- [ ] M7.11 只有 HAKE 单字段变体的证明已闭合后，才考察集合 D：给出顺序删减时前提保持的证明，或直接证明联合变体的 Bad 事件；不能简单相加单字段结论。
- [ ] M7.12 做非循环审查：每个引理的依赖在删减后依然可用，而且适用于定理声称的全部攻击者。
- [ ] M7.13 主结果只陈述 `Π_HAKE−D` 在准确模型、假设和故障分支下的性质。若证明中出现可复用条件，仅作为讨论或后续第二篇的线索；不得将 HAKE 特有假设包装成一般协议类定理。

可使用下式组织证明，但它是账本格式，不是已成立的定理：

\[
\operatorname{Adv}_{\Pi_{\rm HAKE}-D}^{S}(\mathcal A)
\leq \operatorname{Adv}_{\Pi_{\rm HAKE}}^{S}(\mathcal B)
+\varepsilon_{\rm bind}+\varepsilon_{\rm auth}
+\varepsilon_{\rm state}+\varepsilon_{\rm kdf}.
\]

实际证明可能直接调用底层假设而不归约到原协议；无论选哪条路径，都必须解释每个损失项的来源、适用攻击者及查询依赖。若混合鲁棒性要求某分支不依赖 KEM 假设，不能把一个无条件相加的 KEM-binding 项藏进该分支。

产物：`hake-reduced-variant.md`、HAKE-specific `proof.md`、`assumption-ledger.csv`、逐分支 security ledger；如成立，再增加联合删减变体证明。

验收：没有 TODO 的核心逻辑跳步；每一项安全合同都有证明或清晰的未覆盖标记。若定理只能覆盖较窄保证，必须同步修改摘要、题目和贡献陈述。

## 12. M8：用形式化工具验证协议层结果

**回答的问题：复杂会话交织和状态条件是否遗漏，抽象是否把结论假设进去了？**

本阶段是 **protocol-level validation**，不是 computational primitive proof。它只在 M5–M7 的 HAKE 证明路线已经明确后开展，不得抢占具体变体证明和分离结果的时间。建议先固定一种协议工具；已有项目工具若能表达这些目标就继续使用，没有既定模型时可先用 Tamarin 表达并发、腐化时序和状态。不要同时搭四套模型来替代理论进展。

- [ ] M8.1 建立忠实原版模型，写 reachability lemma，确认各角色能按原规则完成。
- [ ] M8.2 明确 KEM 抽象。若 constructor 把 pk、ct 自动编码进 key，记录其引入了哪些 binding，不得把这当作底层实例已经证明。
- [ ] M8.3 建模可信凭据映射、QKD 一次输出、失败状态、MAC 检查、secret 擦除与腐化时点。
- [ ] M8.4 明确事件参数：身份、角色、sid、public-key tuple、贡献/上下文摘要、最终 key。模型内允许记录秘密作事件参数，不等于线上泄露。
- [ ] M8.5 验证原模型中适用的 secrecy、agreement、单向 confirmation、PFS 等 lemma；工具目标不得比声明含糊。
- [ ] M8.6 修改字段投影得到变体，保持其余模型一致；保存精确差异。
- [ ] M8.7 至少做一个与主假设相关的负面对照，检查去掉关键保证时是否能产生预期反例，防止模型把攻击空间锁死。
- [ ] M8.8 记录工具版本、运行命令、模型版本、每个 lemma 的 verified/falsified/timeout 状态和 trace。
- [ ] M8.9 将工具结果与计算证明逐项映射；未找到攻击不等于证明，timeout 不等于安全。
- [ ] M8.10 如需机器核验计算归约，再考虑 EasyCrypt 等；不把这作为当前完成论文的无条件附加负担。
- [ ] M8.11 在结论中显式划界：Tamarin 可支持多会话、identity agreement、session mixup、认证对应、corrupt timing 与删减后的 trace；它不能证明 C2PRI/K–PK/K–CT 的概率安全，也不能替代 multi-input KDF 归约。

产物：原版/变体模型、运行说明、验证日志、反例 trace、抽象假设清单。

验收：模型可重跑；诚实路径可达；负面对照有效；符号结果没有被用来宣称直接证明 KDF 伪随机性或信息论安全。

## 13. M9：HAKE 联合删减与成本评估

**回答的问题：已证明的 HAKE 变体能删到哪里，实际省了什么？**

本阶段优先级低于 HAKE proof、counterexample 和必要的 protocol-level validation。若前述闭环尚未成立，只维护成本计算模板，不启动大规模 benchmark。

- [ ] M9.1 根据已证明的规则组合删减集合，重查依赖和安全分支。
- [ ] M9.2 结果命名使用“已证明安全的 reduced context”；只有建立相应最小性论证才能写“最小”。
- [ ] M9.3 区分 inclusion-minimal（没有真子集仍满足目标）和 minimum-cost（在规定候选空间及成本函数下全局最优）。单字段删除失败也不自动排除更大联合删减。
- [ ] M9.4 先算精确 KDF 输入字节变化，列出字段编码、长度前缀、重复散列与调用次数。
- [ ] M9.5 比较实际构造中的散列/置换调用、KDF 耗时、内存和完整握手耗时；报告重复次数、硬件、软件版本、波动。
- [ ] M9.6 线上报文未变化时，不声称减少通信字节；大公钥不再进入 KDF也不代表大公钥不再发送。
- [ ] M9.7 只改删减点，固定 KEM、参数、编译条件和测试负载；模拟 QKD 必须明确标记。
- [ ] M9.8 性能收益很小时，忠实报告；理论贡献应由定理和边界支撑。

第二协议实例及跨协议充分条件不在第一篇 M9；如 HAKE 分析自然产生可复用条件，只记录为后续第二篇研究线索，不在当前闭环中验证另一协议。

产物：HAKE `elision-results.csv`、成本表；若开展 benchmark，再附基准脚本和原始数据。

验收：每个省略字段都有前提与证据；每项性能数字可复现；没有未经证明的最小性或通用性结论。

## 14. M10：组织论文并审查可发表的研究闭环

**回答的问题：读者能否从问题一路追到可核验结果？**

论文建议目录如下，写作时依据投稿篇幅合并章节：

| 章节 | 要回答的问题 | 必须放入的内容 | 依赖产物 |
|---|---|---|---|
| 1 Introduction | 为什么显式重复绑定值得研究，难点何在？ | 具体问题、结果范围、已实现贡献 | M0、M2、M7 |
| 2 Related Work | 与已有 KEM/combiner 省略结果相比新增什么？ | 最近邻定理逐项比较 | M2 |
| 3 HAKE Model and Preliminaries | 安全是什么意思，攻击者能做什么？ | Figure 3 基线、原语游戏、freshness、故障分支 | M1、M3 |
| 4 HAKE Context and Reduced Variants | 具体删哪个出现位置，原字段承担什么作用？ | 原式/投影式、单字段变体、字段职责和证明依赖 | M4、M5 |
| 5 Security Preservation for Reduced HAKE | 该具体变体为什么安全？ | HAKE-specific 正确性与安全证明、界；联合删减成立时再证明联合变体 | M7 |
| 6 Limits and Counterexamples | HAKE 中哪些删减直觉不成立？ | 适用模型内的反例、未覆盖条件和必要假设边界 | M6 |
| 7 Hybrid-Branch Security Ledger | 原 HAKE 哪些保证被保留或收窄？ | 两个故障分支、条件 ITS、认证/确认及新增假设逐项对照 | M3、M7 |
| 8 Validation and Evaluation | 结果怎样核验，成本变化多大？ | 模型假设、验证结果、成本与实验 | M8、M9 |
| 9 Discussion and Conclusion | 有效范围和未解决问题是什么？ | ITS/实例限制、假设代价；跨协议定理作为后续第二篇方向 | 全部 |
| 附录与 artifact | 他人能否重现？ | 完整游戏、长证明、模型、反例、命令和数据 | 全部 |

推荐实际写作顺序：HAKE baseline → Reduced Variant → Security Proof → Boundary/Counterexamples → Security Ledger → Evaluation → Related Work → Introduction → Abstract。工作日志从 M0 开始写，摘要等结果稳定后再写。

- [ ] M10.1 每条 contribution 都能指向一个定理、反例或实例化结果。
- [ ] M10.2 检查“首次、通用、最小、完全保持”等词是否有证据。
- [ ] M10.3 检查新增假设是否削弱原混合安全或攻击模型，并在主文显著说明。
- [ ] M10.4 区分原论文声明、本研究核验、本研究扩展；不把复现当作新增定理。
- [ ] M10.5 交叉检查论文公式、模型字段和实现输入完全一致。
- [ ] M10.6 重跑必要验证与性能复现，保留失败及超时记录。
- [ ] M10.7 在选定 venue 后再按官方最新规则整理匿名、页数和 artifact；本大纲不预设录用级别。
- [ ] M10.8 在主文加入 security ledger，逐项并列原 HAKE、Reduced HAKE、是否保持、所需新增假设和未覆盖分支；不能只给一个总的“安全保持”结论。

Security ledger 至少包含：

| 性质/分支 | 原 HAKE 声明 | Reduced HAKE 结论 | 新增假设 | 证据或缺口 |
|---|---|---|---|---|
| SK security | 待按原文固定 | 待证明 | 待填 | 待填 |
| PFS | 待按原文固定 | 待证明 | 待填 | 待填 |
| KEM-secure / QKD-failed | 待按原文固定 | 待证明 | 待填 | 待填 |
| QKD-secure / KEM-secrecy-failed | 待按原文固定 | 待证明 | 待填 | 待填 |
| QKD-secure / KEM-binding-failed | 待按原文确认是否覆盖 | 待证明或明确不继承 | 待填 | 待填 |
| conditional ITS | 待按原文固定 | 待证明或明确不继承 | 待填 | 待填 |
| authentication / key confirmation | 待按实际方向固定 | 待证明 | 待填 | 待填 |

验收：读者不依赖聊天记录，也能明确理解主张、假设、证明和限制。

## 15. 五个必须作出判断的关卡

| 关卡 | 通过条件 | 不通过时如何处理 |
|---|---|---|
| G1：查新，M2 后 | 找到非平凡差异和新增证明义务 | 收缩/调整问题，不先写“创新点已成立” |
| G2：Hybrid 分支兼容性，M3 后 | 候选的替代假设在每个原承诺故障分支中均可合法使用，或已明确接受较窄合同 | 保留该字段、换候选，或将主张改为“额外 binding 假设下成立” |
| G3：单字段，M5–M6 后 | 至少一个候选有合理证明路线，或有有价值分离结果 | 修改条件、保留字段，或转向边界研究 |
| G4：HAKE 变体安全证明，M7 后 | 具体单字段变体的非循环证明成立，安全分支覆盖清楚 | 承认较窄保证，或撤回该删减方案 |
| G5：组合与评估，M9 后 | 联合删减前提经重新检查，存在可复现结果 | 只发表单字段/边界结果，不用实验包装未证联合删减 |

不要用增加跑分、添加图表或多跑几个工具掩盖 HAKE 变体安全证明缺口。每个失败都应缩小结论范围或产生具体反例。

## 16. 从现在开始的前十个工作单元

这里按产物而非固定日历推进；证明耗时不宜承诺精确周数。

1. 读原论文 Section 3.1 和 Figure 3，交付符号表与消息表。
2. 交付 KDF 数据流与 MAC 覆盖表；标出重复出现的字段。
3. 读原文安全定义及定理，交付原始安全合同。
4. 对首批候选填写 Hybrid failure-branch compatibility 表，明确“secrecy 失效”与“binding 失效”的不同口径，作出 G2 判断。
5. 完成最接近的 KEM/combiner 删减结果对照，交付一页 novelty-gap。
6. 完成 field-ledger：每个字段至少填生成者、位置、作用、依赖、候选替代、故障分支兼容性和风险。
7. 只从 G2 准入项中写出一个单字段变体，检查实际 combiner 游戏是否允许删减；不直接改最终方案。
8. 为该 HAKE 变体写定义、Bad 事件及单字段安全保持证明草稿；若无进展，记录阻塞点并换下一个准入候选。
9. 建立一条针对核心条件的 HAKE separation/攻击卡，优先检验 primitive binding 是否不足以替代 identity/role/session 语义。
10. 作出 G3 判断：继续 HAKE 单字段安全命题、补必要条件、换候选，或转为边界结果。只有具体证明路线明确后再建立最小形式模型；联合删减和性能实验继续后置。

完成这十项之后，先闭合 HAKE 单字段变体的安全证明，再按顺序考虑形式验证、联合删减和性能实验。眼下最优先的实际动作是第 1–4 项，因为字段的真实作用、原安全合同与 Hybrid 分支兼容性会决定后续每一个判断。

## 17. 可直接复制的字段审查模板

```text
字段/出现位置：
原协议版本与证据页码：
生成者与获得方式：
公开/秘密/状态值：
编码与长度：
哪些消息、MAC、KDF 层使用：
希望保留的性质：
候选替代保证及其精确游戏：
保证适用的攻击者与泄漏条件：
保证成立的时间：
是否依赖当前字段或其他拟删字段：
KEM 存活分支：
QKD 存活且 KEM secrecy 失效分支：
QKD 存活且 KEM binding 也失效分支：
替代保证是否仍可合法使用：
原条件 ITS 分支：
可能的攻击及模型合法性：
计算证明义务：
形式模型 lemma：
已完成证据位置：
结论：已证明可删 / 附加条件下可删 / 已有反例 / 证据不足
下一步最小任务：
```

## 18. 核对来源与优先阅读入口

本大纲的协议与安全声明以仓库中的原论文 PDF 为准；以下外部入口用于后续核对最近邻工作，不表示已完成全面查新。

- Clermont–Henrich：[The Best of Both Worlds: Hybrid Authenticated Key Exchange for QKD(N) without Signatures](https://eprint.iacr.org/2026/1231)。本次读取的是项目中的 PDF；后续实验和引用需继续固定版本。
- Backendal 等：[Key Derivation Functions Without a Grain of Salt](https://doi.org/10.1007/978-3-031-91101-9_14)。原 HAKE 采用的 KDF 框架，作为 M2 的必读材料。
- CFRG：[Hybrid PQ/T Key Encapsulation Mechanisms，draft-11](https://www.ietf.org/archive/id/draft-irtf-cfrg-hybrid-kems-11.html)。本次核对了 C2PRI、binding 以及 CG/CK 省略输入的相关段落；它是草案。
- Cremers–Dax–Medinger：[Keeping Up with the KEMs](https://eprint.iacr.org/2023/1933.pdf)。作为 M2/M5 的 binding 原始定义核对入口。
- [Starfighters — on the general applicability of X-Wing](https://eprint.iacr.org/2025/1397)。作为 M2 的近邻工作核对入口。

第一篇预期结论的形式应当是：“对原 `Π_HAKE` 的具体 context 位置集合 D，在模型 M、假设 H 和明确故障分支下，变体 `Π_HAKE−D` 保持性质 S，损失为给出的界；不满足某项条件时，给出相应 HAKE 边界或反例。”其中 H、M、D、S 都需要通过后续工作确定，不能提前填成有利结论。跨协议的 `security guarantee → context elision` 定理明确留待第二篇。
