# M4.5 Summary：Binding Layer Separation

日期：2026-09-19。状态：M4.5 分层分析完成，等待审查；M5 未开始。

## 已完成

- 建立 L1 Primitive Binding、L2 Protocol Binding、L3 Hybrid Composition Binding 的分析分类。层不是互斥集合，也不是新的安全模型或自动蕴含关系。
- 覆盖 A、B、pk_a、pk_b、pk_e、k1、k2、k*、qkdKeyId 共 9 个字段、11 个 context 位置。A/B 的两处记录分行，k* 的 secret/context 角色分开说明。
- 逐位置登记出现位置、绑定对象、所属层、依赖性质、已有候选保证及其范围、跨层缺口、来源和已有证明义务。
- 保留角色本地值、MAC key/message 区别、原 QKD 检索/⊥/一次性语义及 BG/KO/QS/QB/QT/FF 分支。

## 核心回答

**不能把 primitive-level binding 直接当作 protocol-level binding。** K-PK/K-CT 与 C2PRI 限定的是各自游戏中的原语对象关系，不直接定义 HAKE 身份、角色、matching、接受、确认或 QKD 同步。

已有保证可能成为局部证明前提；从这些前提到 HAKE 协议语义，再到混合组合语义，仍需可审查的 protocol-level proof bridge。本轮没有建立该桥，也没有证明这种桥不可能存在。

## 尚待审查

1. 具体 KEM 实例与精确 binding 游戏、生成/泄漏域及 PPT/QPT 范围是否匹配。
2. 公钥/组件 key 与逻辑身份、角色、SID 和本地会话之间的对应。
3. τ1/τ2 可用时点与前提；τ3 不得循环支持 F 前的关联。
4. QKD 应用端点映射、source/α/查询索引及最终输出拆分接口。
5. 各失效分支中哪些保证确实存活；QS 仍是较窄候选假设，QT 不借用计算性 binding。

OI-01–OI-06 与 B1–B7 均未因本轮分类而关闭。Identity agreement 等词汇只描述候选辅助职责，不作为新加入的安全目标。不把原论文 PFS 声称升级为本项目已证明的强 PFS/PCS。

## 交付与停止范围

- [binding-layer-analysis.md](binding-layer-analysis.md)：来源、三层定义、逐字段分析、跨层缺口及分支约束。
- [binding-layer-matrix.csv](binding-layer-matrix.csv)：11 行位置级审查矩阵，全部状态为 requires validation。
- 本摘要：结论和人工审查入口。

仅新增以上三份文件，不修改已有 M4 映射、路线图、协议、模型或来源。表格按科研记录规范采用平面 CSV，显式保留来源与不确定状态，不以颜色或评分代替证据。

验证记录：已有 M2–M4 文档校验通过，10 份固定来源哈希匹配；新 CSV 经两种读取方式检查，11 个位置、9 个字段、12 列，无空单元或意外公式，状态一致；新增 Markdown 本地链接均可解析。CSV 是纯文本记录，不提供工作簿样式或公式，未另导出 XLSX/预览文件。这些检查仅验证文档结构及一致性，不是安全证明。

本轮未删除字段、未构造 reduced HAKE、未写安全定理、未宣称字段冗余，未运行攻击搜索或 Tamarin。到此停止，等待 M4.5 审查，不自动进入 M5。
