# M7.5 Proof Interface Closure Report

状态：接口**定义闭合**，接口**证明/满足未闭合**。停止等待M7.5审查，不进入M8。

## 1. C1是否仍有可证明路径？

**有一条条件性、尚不可执行的证明架构：**

```text
合法HAKE记录中的secret/context等值
        ↓  L_C1-WF / ENC
original–reduced完整输入与查询映射
        ↓  L_C1-SRC / QRY
适用KDF idealization
        ↓  L_C1-CONF / PROT / LOSS
Option C：Key-IND + matching correctness + 原PFS，逐分支报告
```

“仍有路径”只表示没有在本轮发现逻辑上必然失败；不表示C1成功、现有KDF定理可直接使用或损失可忽略。一个有效路径可能采用existing theorem的合法实例化，也可能需要new HAKE-specific Secret–Context Elision Lemma；当前证据更支持后者。

## 2. 最大阻塞

最大阻塞是 **secret-bearing context 与 adaptive KDF/RO query interface 的联合不兼容**：

- K25要求c可从公开α提取，C1后c_KEM仍含秘密k1/k2；不能公开它们来套定理。
- 合法协议记录中的 `context tail = σ_KEM` 只给well-formed结构关系，未覆盖攻击者登记、任意获准RO查询、重复/匹配调用和cross-domain查询。
- 即使source接口解决，τ3公开依赖前半输出，最终Test使用后半输出，仍需联合视图证明。

第二阻塞是OI-01–06尚未冻结，使Option C中的正式完成点和PFS历史不能成为可执行实验。B2/B6还阻塞QT与量子/无界接口。

## 3. 安全目标决定

选择 **Option C：Key secrecy + matching + PFS** 作为primary theorem target，因为H26的freshness已经包含PFS，Def.6的SK-security已经包含matching正确性。选择A/B并排除这些语义会要求补造一个原文没有的弱化模型。

Option A/B可以作为未来证明分解中的中间lemma，但不能单独称C1保持原HAKE安全。Hybrid robustness按环境/分支覆盖，而不是另一个可被忽略的性质。

## 4. Existing theorem还是new lemma？

- **Existing KDF theorem：当前不能直接使用。** public context、source/query映射和确认接口未满足。
- **Preferred research route：new HAKE-specific interface lemma。** 范围只限C1，条件为well-formed记录、canonical encoding lift、真实α、adaptive query simulation、确认/协议提升和逐分支损失。
- **保留可能性：** 若以后找到不公开秘密、不重分类输入且完全满足K25的faithful instantiation，可改用existing theorem。当前不宣称这一可能性已被排除。

## 5. 是否进入M8？

**否。当前不满足进入M8的条件。** 至少需要：

1. 人工审查并接受 `L_C1-WF/ENC/SRC/QRY/CONF/PROT/LOSS` 的接口范围；
2. 裁决证明实际需要的OI-01–06，或明确哪些未定义项使主目标无法陈述；
3. 确认existing-theorem实例化路线是否可行，否则批准仅HAKE-specific的新lemma路线；
4. 给G0→G1选择非循环的线性或双路径实验结构，并使损失项具备可定义来源；
5. 明确所声称覆盖的BG/KO/QB/QT范围，不能用QS附加条件替代原范围。

## 6. 新增文件与边界

- [secret-context-overlap-analysis.md](secret-context-overlap-analysis.md)
- [kdf-interface-compatibility.md](kdf-interface-compatibility.md)
- [security-target-selection.md](security-target-selection.md)
- [m7.5-theorem-update.md](m7.5-theorem-update.md)
- 本报告

没有写完整theorem proof、没有修改HAKE、没有删除字段、没有证明pk_e、没有使用C2PRI替代context binding、没有扩展到其他协议，也没有运行Tamarin。M7既有文件和来源保持不变。

