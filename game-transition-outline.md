# M6：Game Transition Outline（未执行）

本文件只给证明路线和失败接口，**所有转换损失未知、所有安全 hop 未验证**。没有完整 theorem、归约算法、可执行安全游戏或攻击搜索。对 j=1、2 分别应用，不组合两项候选。

## 1. 不把直接替换当作免费一步

用户给出的 Game 0→Game 1 示例可登记为：

| 路线节点 | 操作意图 | assumption | reduction loss | unresolved issue |
|---|---|---|---|---|
| G0-O | Original HAKE 的原目标实验 | H 明示规则及待裁决的 OI；基线source合法性 | 非转换；基线本身未完成接口验证 | 完整 experiment 尚非可执行定义 |
| G1-Cj | 只按 pj 换本地 context 的候选实验 | 不能只凭“其余程序相同”认定视图接近 | Δdirect,j = unknown；没有上界 | F、tag和后续中止可以改变；不得设为0 |
| G2-Cj | 尝试在候选侧使用必要的 primitive/protocol 对应依据 | C1未必需要K-PK；C2需精确游戏域和独立L2桥 | Δbridge,j,b = unknown | 对b=QB/QT，计算binding路径不可用；不强行保留此hop |
| G3-Cj | 试图将适格派生记录的KDF输出理想化 | 合法source/α、查询req、oracle能力、逐分支存活前提 | Δkdf,j,b = unknown | B1–B6；匹配/重复调用需保持一致，不能每次调用独立随机 |

这些节点是“希望解释的差分”，不是已成立的游戏序列。特别是从原到候选的第一步本身可能包含全部研究难点。不能在其后引用 binding，再追认第一步损失为零。

## 2. 优先审查的双路径接口

可先尝试分别分析原/候选到各自理想实验，而不是预设它们逐字等价：

```text
O-real  ...待证source/协议接口...  O-ideal
Cj-real ...待证source/协议接口... Cj-ideal
O-ideal ...待证记录/查询对应...   Cj-ideal
```

这只是 proof dependency 示意。两侧理想实验是否可构造、是否等价或仅在目标优势上可比较，仍未确定。若此路线要求比原安全保持更强的完整视图等价，应停止并重新界定所需目标，不擅自增强主张。

| 接口 | 必要审查 | 归约损失登记 | 停止条件 |
|---|---|---|---|
| O-real→O-source | H→K25 source、α、索引、允许泄漏 | Δsrc,O,b：unknown | B1未闭合，不得引用KDF界 |
| Cj-real→Cj-source | 同样核对候选，不能借用原σ/context映射而不检查 | Δsrc,j,b：unknown | 投影导致的接口变化未解释 |
| source→ideal | 适用req、RO访问方式、source存活条件、预测及碰撞余项 | Δkdf,O/j,b：unknown，不能删余项 | QB/QT中使用失效假设；经典模拟未经依据提升QROM |
| O-ideal↔Cj-ideal | 有关原目标的记录对应、matching/重复查询及泄漏一致性 | Δmap,j,b：unknown | 输入别名未处理或需虚构SID唯一性 |
| ideal→最终Test接口 | 512-bit输出、确认半段tag及最终半段挑战的共同视图 | Δconfirm,O/j,b：unknown | 假定独立输出而未验证，或改变Test/freshness |
| 全路径→原PFS/分支主张 | 历史状态/擦除/腐化语义一致及范围完整 | Δhistory,j,b：unknown | 默许完美erase、增强假设或丢弃失败分支 |

所有 Δ 都是待填写损失槽位，不是已经定义的优势界；本轮不将其相加写成安全定理。必须先确认实验、归约接口和优势 convention，再确定是否有可组合的损失账本。

## 3. 两候选的区别

### C1

合法本地完整输入仍有 σ_KEM，原重复项可在元组描述中恢复；因此“少一个字符串所以必有新别名”也不能直接成立。需要证明的不是所有 reduced context 都唯一，而是与目标有关的完整输入和查询关系是否足够保持。

但元组重建不提供任意 RO 输入的重命名方案：对手可直接访问被允许的 RO 接口，模拟需覆盖非协议生成字符串、编码域、重复查询、以及原/候选相关的查询。不得把结构观察升级为两个 F 输出相同，也不得未经模拟就编程 RO 或忽略其可见性。

### C2

候选完整输入未直接含 pk_e，可能考虑以保留的组件贡献和适域 K-PK 限制相关多对一情况。进入该讨论前必须建立哪两次合法调用对应游戏对象；随后仍要连接协议身份/会话与时序。

C2PRI 是固定 pk 的第二原像性质，不能填充跨 pk_e 这一步。QKD-only 且无 binding 前提的 QB、无界 QT 不能沿同一计算绑定路线继续。τ3 含 pk_e 也不是免费解决方案，因为确认 key 已受候选 F 影响。

## 4. 非可执行的潜在失败类别

义务表 Potential counterexample 列仅列需排查的抽象类别，不是已发现反例或运行步骤：查询记录误合并、来源/角色映射缺失、合法泄漏不一致、后置确认循环、错误沿用故障组件保证。

一个真实的分离结论将来还需证明相关执行在原 H 能力和 freshness 内合法、违反原目标而不是新增强性质，并排除模型补全造成的伪差异。本轮不构造这样的执行，不增加攻击者能力。

## 5. 当前可交付结论

已识别两条候选路线及各自阻塞接口，尚无有效完整 game-hop chain，所有损失未知。先审查 SP-01/SP-02 和所需分支范围，再决定是否有依据进入形式化定理细化。停在 M5+M6 文档审查，不进入 M7。
