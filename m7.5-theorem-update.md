# M7.5：Theorem Specification Update

状态：C1定理候选经接口分析更新；不是最终冻结的定理，不是证明。

## 1. Theorem candidate

记Π和Π′按[m7-theorem-definition.md](m7-theorem-definition.md)定义，仅有本地 `c_KEM[8]=k*` 的context occurrence差分，σ_KEM=k*及其他协议操作全部保留。

**Candidate T-C1（待审）：** 在同一经审查的HAKE模型M和同一获准failure环境族E中，如果：

1. original与reduced协议输入均满足一个经证明的HAKE-specific Secret–Context Elision Interface `L_C1`；
2. KDF/source结果在该接口下合法适用；
3. 协议会话、查询、泄漏和确认视图可非循环地提升到该接口；

则Π′满足H26 Def.6的matching正确性和Key-IND，并覆盖M中H原PFS freshness允许的历史；对每个声称保留的hybrid分支分别给出实际适用的损失界。

Key-IND部分的目标形式仍是：对每个获准对手A，构造符合原实验的B，使

`Adv_SK(Π′,M,E,A) ≤ Adv_SK(Π,M,E,B) + ε_interface + ε_KDF + ε_protocol + ε_confirm`。

各ε目前仅是损失类别，不是已定义或已界定的数；不能求和后宣称成立。QT若最终纳入，必须使用与其能力匹配的独立陈述，不能强行塞进同一计算性不等式。

`L_C1`不是可以直接假设的安全结论。它是下一步必须陈述并证明的接口lemma；若其前提重述了Π′安全、公开了秘密、改变了F或排除了原合法查询，则T-C1无效。

## 2. Security property

Primary target为[目标选择](security-target-selection.md)的Option C：

- final `k_h^2` Key-IND，使用H原freshness口径；
- H Def.3 matching及Def.6的matching完成双方key一致；
- 原PFS历史，不扩展PCS或完美erase；
- hybrid robustness作为环境覆盖范围逐分支登记，不把QS窄条件或FF误称原保持。

不新增独立identity/injective agreement、双向确认或pk_e/C2性质。

## 3. Established / Required / Unknown

### Established：已有模型/规范支持

- 原Π输入位置、σ_KEM=k*、两source、固定label和512-bit输出拆分来自H26 Fig.3/§3.1。
- C1程序差分已唯一限定：只改变两角色本地KDF context的末项，其他操作定义不变。
- H Def.3给matching关系；Def.6把matching正确性与Key-IND列为SK-security条件；Def.4文字明确其freshness包含PFS。
- K25提供 `(σ,c,α)` source、source collection、real-or-random查询和req框架；但只确认模型要求，不确认HAKE已满足。
- 原合法本地记录中，待略去context项与保留的σ_KEM取值相同。仅为结构事实。

### Required：必须证明的接口

- `L_C1-WF`：合法记录语言及 `c_KEM[8]=σ_KEM`，不扩展到任意攻击者输入。
- `L_C1-ENC`：canonical/injective original–reduced编码lift，保持目标所需查询等价关系与domain separation。
- `L_C1-SRC`：不泄露k1/k2/k*的source/α兼容性，或一个明确允许secret-bearing context的HAKE-specific lemma。
- `L_C1-QRY`：NewKey/SetKey/Ro$-KDF/KDF、并发协议会话、重复/匹配调用及任何获准RO查询的自适应模拟。
- `L_C1-CONF`：τ3公开依赖确认半段后，最终半段的Key-IND视图和双方完成关系。
- `L_C1-PROT`：matching、Reveal/State/Corrupt/Test、QKD记录和failure branches的协议提升。
- `L_C1-LOSS`：归约对手、资源开销、bad events和非平凡损失界；不能用unknown或平凡界收尾。

### Unknown：尚未解决/需人工裁决

- OI-01–06：Accept/Complete、Test/历史key、Expire/Corrupt、泄漏后控制、SID域、擦除。
- RO/QROM能力及B2预测/碰撞余项；QT是否能在不新增隐藏条件下获得所声称界。
- 现有KDF theorem能否经不改变HAKE的faithful instantiation使用。当前证据指向需要new HAKE-specific interface lemma，但尚未证明“不可能实例化”。
- 实际拼接编码、长度前缀/类型域以及original/reduced cross-domain查询的可界定性。
- ε各项、资源开销、组件正确性误差和完整hybrid覆盖范围。

## 4. Assumptions update

沿用M7表中的A01–A07作为existing assumptions，但A06只是外部定理条件，不表示HAKE满足。X01/X02仍是未采用的additional assumptions。`L_C1-*`全部归类为Required proof obligations，不进入Assumptions栏。

不引入C2PRI/K-PK、完美identity binding、完美erase、unique SID实现、额外攻击者能力或新KDF。若后续证明需要附加条件，必须先更新假设表并等待人工范围审查。

## 5. Game-hop update

G0→G1不再描述成可直接界定的“context replacement hop”。它是需要 `L_C1-ENC/QRY/CONF` 支持的核心比较；直接差分仍unknown。G1→G2负责source/记录接口，依赖 `L_C1-SRC/PROT`；G2→G3才可能调用经验证的KDF安全结果并产生具体损失。

若线性顺序造成循环，应使用“original与reduced分别到各自ideal view，再比较ideal records”的双路径结构；这仍要求两侧source合法，不能绕过B1。任何路线均不得假定两协议输出/tag相同。

## 6. 更新结论

M7定理候选已从一个带unknown损失的宽泛保持式，收紧为“Option C原目标 + HAKE-specific L_C1接口 + 逐分支损失”的待审规格。接口名称和条件已明确，但接口尚未满足，所以最终定理仍未冻结。

