# M7：C1 Game-hop Specification（未证明）

只规定G0–G3的差分、前提和状态；不构造归约算法，不计算优势上界，不运行工具。公共模型M和环境E见[m7-theorem-definition.md](m7-theorem-definition.md)。M未冻结前各Game仅为带接口占位的实验规格。

## 1. Game定义与转换记录

| Game / transition | difference | assumption | status |
|---|---|---|---|
| G0：Original HAKE | 运行原Π；Test指向其最终k_h^2 | A01–07中实际适用的原前提；完整M；原source接口也须合法 | specification only；O01–12相关项未闭合 |
| G0→G1：Context reduction | 两角色本地F调用仅去掉c_KEM[8]，σ_KEM保持；运行候选Π′的原控制规则，使用其派生key生成τ3 | 不能仅凭σ仍在就认定hop有效；需C1相关输入/查询/确认分析 | **unproved**；差分unknown，不设为0 |
| G1→G2：Use required binding/source assumptions | 目标是把候选执行组织成经验证的source/会话/查询记录。没有预设修改消息或换原语 | O07/O08/O11；如确需primitive结果须先给适域理由；X01/X02未采用 | **blocked**；当前没有可执行的G2变换或已获界的bad事件 |
| G2→G3：Ideal KDF | 拟将满足所引用KDF游戏条件的逻辑派生记录映射为一致的理想输出；每个输出仍拆成两半并按原规则处理确认 | 合法source/α/req、正确oracle能力、存活源及确认视图；不替换不适格输入 | **blocked**；O07–10未解决，理想化损失unknown |

G2不是“加入binding假设就算转换完成”。如果该步只是重记账，需要证明视图保持；如果需要终止于某bad事件，需定义事件、证明它涵盖有关差异并给出界。两种实现本轮均未选定。

G3不是“每次F调用独立随机”：匹配双方和合法重复调用所需一致性必须按照经过验证的KDF接口保留。不能把整个512-bit值返回Test，也不能因切成两段就省略τ3对视图的影响。

## 2. 差分记账及量词限制

待所有实验定义完整后，对同一待比较对手策略可使用记账符号：

`δ_i := |Pr[G_i outputs win] − Pr[G_(i+1) outputs win]|`，i=0,1,2。

目前三个δ均unknown；这个记号不提供可用上界，也不证明定理规格中“对每个A存在B”的归约关系。各概率需要其完整实验与随机源先被定义，不能把符号本身当作测量结果。

逐项待补内容：

- δ0：原/候选不同输入如何对应、任意获准oracle查询、合法泄漏、tag与自适应中止如何处理。
- δ1：source重表达的准确性，是否需要bad事件，其作用域与归约/概率界；不是泛称Bad_alias即可。
- δ2：实际KDF结果的版本、source条件、req、资源开销、预测/碰撞项及优势归一化。
- G3终点：最终Test视图是否真正给出所需随机性；合法Reveal、确认半段和状态查询仍要解释。不能提前写成功率恰为1/2。

T-MATCH与T-PFS并非上述隐藏位差分自动涵盖：前者还需核对完成会话的一致性事件，后者要求M与模拟真正保留原历史暴露资格。

## 3. 此线性顺序的逻辑风险

G0→G1已经是核心安全差分，不能先假定它很小，再用G1→G3证明来补回前提。后续source/binding分析若用于界定δ0，必须独立成立，不依赖C1结论。

若直接hop难以建立，可另审原/候选各自到适格理想实验的比较路线（见[M6路线](game-transition-outline.md)）；两边的source接口均需证明。该备选不表示本轮新增Game、开始证明或绕过原基线问题。

## 4. C1结构观察的使用上限

原合法同侧元组中c_KEM末项等于σ_KEM。候选完整元组保留σ，因此有结构恢复的研究依据，但不等于：

- 所有会话的k*相同或唯一；
- 两个协议F输出相等；
- 任意字节编码、RO查询和source索引均存在已证明的双射；
- 攻击者视图或合法泄漏完全相同；
- KDF定理可直接用于secret-bearing context。

不同reduced context的统计/语法关系、完整输入别名与输出碰撞分别分析。不能只凭少一项就声称新增碰撞，也不能只凭可恢复就认定全部δ为0。

## 5. 分支适用性与终止点

BG/KO仍需source和协议映射；KO不调用理想QKD同步结论。QB中任何计算binding步骤都不能使用；QT还需单列能力和余项，不能复用计算性MAC安全。QS是未采用的窄附加环境，FF不补造非平凡安全目标。

因此本轮提供的是带差分、假设和阻塞原因的四Game规格；没有一个完整有效hop chain，也没有证明完成。等待M7审查后才可决定如何填实各接口。
